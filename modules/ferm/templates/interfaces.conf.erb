def $MUNIN_IPS = (<%= v4ips.split(',').join(' ') %>);

domain ip {
        chain INPUT {
               daddr ($MUNIN_IP) NOP;
        }
}

domain ip {
        chain OUTPUT {
               saddr ($MUNIN_IP) NOP;
        }
}
